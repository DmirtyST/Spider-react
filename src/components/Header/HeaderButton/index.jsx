/** @format */

function HeaderButton({styles}) {
	return (
		<div className={styles.header_button_controller}>
			<button className={styles.controller_button}>
				<span>Join Community</span>
			</button>
		</div>
	);
}

export default HeaderButton;
